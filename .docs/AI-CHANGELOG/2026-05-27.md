## [Log Date: 2026-05-27] | By: AI Agent

### Component: Test Dashboard — Hybrid Runtime & Ephemeral Execution Architecture
- **Action:** พัฒนาสถาปัตยกรรมระบบทดสอบแบบ Hybrid โดยใช้ Node.js Worker ร่วมกับ Ephemeral Docker Containers เพื่อความปลอดภัยและการแยกส่วน (Isolation) ในการรันเทส
- **Action:** ติดตั้งระบบคิวงาน (Job Queue) ด้วย **BullMQ (Redis)** เพื่อจัดการลำดับการรันเทสแบบ Asynchronous ป้องกันปัญหาการแย่งทรัพยากรเครื่อง
- **Action:** พัฒนาระบบ **Live Terminal Streaming**:
  - ใช้ Socket.IO ในการดูด Log สดๆ จาก Container ส่งตรงไปยัง Angular Dashboard แบบ Real-time เพื่อให้ผู้ใช้เห็นสถานะการรันเทสในทันที
- **Action:** บูรณาการระบบ **Artifact Extraction (Memory-Only Flow)**:
  - ใช้ `tar-stream` เพื่อดึงไฟล์ผลลัพธ์การทดสอบ (.json / .trx) ออกจากตู้คอนเทนเนอร์เข้าสู่หน่วยความจำของ Worker โดยตรง (Memory Extraction) ก่อนสั่งทำลาย Container
- **Action:** เสริมความปลอดภัยด้วยระบบ **Sandbox Policy**:
  - ตรวจสอบและควบคุมคำสั่งรันเทสผ่าน `sandbox-policy.json` เพื่อให้มั่นใจว่ารันเฉพาะ Image และ Command ที่ได้รับอนุญาตเท่านั้น
- **Action:** เพิ่มเอกสาร Workflow Diagrams ใน `scripts.test/test-dashboard/README.md` โดยใช้ **Mermaid.js** เพื่ออธิบาย Flow การทำงานตั้งแต่ต้นจนจบ (E2E Execution Flow) และการไหลของข้อมูล (File-Level Data Flow)

### Component: Infrastructure — Testing & Cleanup Logic
- **Action:** ติดตั้งกลไก **Strict Cleanup**: สั่ง `container.remove()` ทันทีหลังจบภารกิจ เพื่อให้มั่นใจว่าไม่มี Container ตกค้างหรือไฟล์ขยะหลงเหลือใน Host System
- **Action:** รองรับการประมวลผล Report หลากหลายรูปแบบ (XML/TRX/JSON) และนำมาแมปเข้ากับโครงสร้าง `TestCase[]` เพื่อส่งต่อให้ Frontend

### Verification & Quality Assurance
- **Architecture:** รองรับการรันเทสครอบคลุมทั้ง Backend (.NET 8), AI Engine (Pytest), และ Load Test (Node.js) ในสภาวะแวดล้อมที่ถูกตัดขาด (Isolated Environment)
- **Visualization:** ระบบสามารถส่งผลลัพธ์สุดท้ายกลับไปยัง UI เพื่อวาดกราฟสรุปผลด้วย Chart.js ได้อย่างสมบูรณ์
- **Documentation Check:** ไฟล์ `README.md` ในส่วนของ test-dashboard ได้รับการอัปเดต Mermaid Chart แสดงลำดับขั้นตอน 12 ขั้นตอนของ E2E Execution Flow เรียบร้อยแล้ว

---