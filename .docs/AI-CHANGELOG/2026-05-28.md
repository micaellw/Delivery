# AI-Generated Changelog: 2026-05-28

## รายสรุปภาพรวม (Summary)
ในวันนี้มีการปรับปรุงโครงสร้างระดับสถาปัตยกรรม (Architectural Refactoring) ในส่วนของ `admin-dashboard` และ `test-dashboard` โดยเน้นการแยก Logic, Types และ Configuration ออกจากตัว Component หลัก เพื่อเพิ่มความอ่านง่าย (Readability) และการบำรุงรักษา (Maintainability) โดยไม่มีการแก้ไข Logic การทำงานของโค้ดเดิม

---

## 1. ส่วนงาน scripts.test (Test Dashboard)
มีการจัดโครงสร้างใหม่ในส่วนของ Frontend เพื่อรองรับการขยายตัวของระบบทดสอบ:

- **Core Models Separation**:
  - สร้าง `test-dashboard.model.ts` เพื่อเก็บ Type Definitions ทั้งหมด เช่น `TestCase`, `SuiteConfig`, `DashboardStats` และ `TestStatus`
- **Centralized Configuration**:
  - สร้าง `test-dashboard.config.ts` สำหรับเก็บค่าคงที่ของระบบ เช่น รายชื่อ Test Suites (`TEST_SUITES`), ระบบ Health Check (`SYSTEM_HEALTH`) และเมนูนำทาง (`DASHBOARD_NAV`)
- **Component Isolation**:
  - ตรวจสอบและคงสภาพ `SidebarComponent` และ `CaseDetailModalComponent` ให้ทำงานตามโครงสร้างใหม่
  - แยกส่วนการแสดงผล Modal รายละเอียด Test Case ออกเป็น Component อิสระเพื่อลดความซับซ้อนของหน้าหลัก

## 2. ส่วนงาน admin-dashboard
ปรับปรุงส่วน Layout หลักให้เป็นระเบียบตามมาตรฐาน Angular Standalone Component:

- **Main Layout Refactoring**:
  - แยกโครงสร้างเมนูนำทาง (Navigation Items) และชุดไอคอน (Lucide Icons) ออกไปไว้ที่ `main-layout.config.ts`
  - ลดขนาดไฟล์ `main-layout.component.ts` โดยการนำเข้าค่าคงที่จากไฟล์ Config แทนการประกาศแบบ Inline
  - ปรับปรุงการจัดการ State ของ Sidebar ให้กระชับขึ้น

## 3. งานด้าน Testing & Quality Assurance
- **Unit Test Template**:
  - ตรวจสอบไฟล์ `.spec.ts` ในส่วน `store-partner`, `customer` และ `register`
  - ยืนยันความถูกต้องของการใช้ Mock Services (`AuthService`, `ShopService`, `StoreService`) ในการทดสอบ

---

**ผู้บันทึก:** Gemini Code Assist